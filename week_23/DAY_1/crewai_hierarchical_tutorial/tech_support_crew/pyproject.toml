[project]
name = "tech_support_crew"
version = "0.1.0"
description = "tech_support_crew using crewAI"
authors = [{ name = "Your Name", email = "you@example.com" }]
requires-python = ">=3.10,<3.14"
dependencies = [
    "crewai[tools]>=0.157.0,<1.0.0",
]

[project.scripts]
kickoff = "tech_support_crew.main:kickoff"
run_crew = "tech_support_crew.main:kickoff"
plot = "tech_support_crew.main:plot"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.crewai]
type = "flow"
