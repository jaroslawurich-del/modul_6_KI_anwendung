# 🤖 KI Dokumentenassistent (RAG + Ollama + LangGraph)

Ein vollständiger lokaler KI-Assistent für Dokumente mit:

- 🧠 Ollama (LLMs + Embeddings)
- 📄 RAG (Chroma Vector DB)
- 🔗 LangGraph Agent
- 💬 Streamlit UI
- 🐳 Docker Setup

---

# 🚀 Start mit Docker

## 1. Projekt starten

```bash
docker compose up --build